-- 1. 테이블 초기화 (항상 맨 앞에 추가)
DROP TABLE IF EXISTS OFFLINE_SALE;
DROP TABLE IF EXISTS PRODUCT;

-- 2. 상품 정보 테이블 생성 (PK 생략, 모두 NOT NULL)
CREATE TABLE PRODUCT (
    PRODUCT_ID INTEGER NOT NULL,
    PRODUCT_CODE VARCHAR(8) NOT NULL,
    PRICE INTEGER NOT NULL
);

-- 3. 오프라인 판매 정보 테이블 생성 (PK 생략, 모두 NOT NULL)
CREATE TABLE OFFLINE_SALE (
    OFFLINE_SALE_ID INTEGER NOT NULL,
    PRODUCT_ID INTEGER NOT NULL,
    SALES_AMOUNT INTEGER NOT NULL,
    SALES_DATE DATE NOT NULL
);

-- 4. 데이터 삽입 (PRODUCT)
INSERT INTO PRODUCT (PRODUCT_ID, PRODUCT_CODE, PRICE) VALUES
(1, 'A1000011', 15000),
(2, 'A1000045', 8000),
(3, 'C3000002', 42000);

-- 5. 데이터 삽입 (OFFLINE_SALE)
INSERT INTO OFFLINE_SALE (OFFLINE_SALE_ID, PRODUCT_ID, SALES_AMOUNT, SALES_DATE) VALUES
(1, 1, 2, '2022-02-21'),
(2, 1, 2, '2022-03-02'),
(3, 3, 3, '2022-05-01'),
(4, 2, 1, '2022-05-24'),
(5, 1, 2, '2022-07-14'),
(6, 2, 1, '2022-09-22');

-- 6. 삽입된 데이터 최종 확인
SELECT * FROM PRODUCT;
SELECT * FROM OFFLINE_SALE;